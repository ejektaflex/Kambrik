plugins {
    id 'multiloader-neoforge'
}

repositories {
    mavenLocal()
}

dependencies {
    implementation("io.ejekta.percale:percale-neoforge-${minecraft_version}:${percale_version}")
    jarJar("io.ejekta.percale:percale-neoforge-${minecraft_version}:${percale_version}")
}
