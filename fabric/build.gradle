plugins {
    id 'multiloader-fabric'
}

repositories {
    mavenLocal()
}

dependencies {
    implementation("io.ejekta.percale:percale-fabric-${minecraft_version}:${percale_version}")
    include("io.ejekta.percale:percale-fabric-${minecraft_version}:${percale_version}")
    // ModMenu disabled until a 26.1.2-compatible version is available
    // runtimeOnly "com.terraformersmc:modmenu:${mod_menu_version}"
}
