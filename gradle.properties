# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G

# Fabric Properties
	# check these on https://fabricmc.net/use
	minecraft_version=1.16-pre5
	yarn_mappings=1.16-pre5+build.5
	loader_version=0.8.7+build.201

# Mod Properties
	mod_version = 0.1.0
	maven_group = ejektaflex
	archives_base_name = kambrik

# Dependencies
	# currently not on the main fabric site, check on the maven: https://maven.fabricmc.net/net/fabricmc/fabric-api/fabric-api
	fabric_version=0.12.3+build.363-1.16
	fabric_kotlin_version=1.3.61+build.1
